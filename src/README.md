# Legacy folder

All MATLAB entry points and functions now live in the **repository root** (`main.m`, `params.m`, `compute_features.m`, etc.). This folder is kept only to avoid breaking old links; do not add `.m` files here or they may shadow root functions if `src` is on the MATLAB path.
